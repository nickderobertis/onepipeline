{
  "version": "1.1.0",
  "description": "The planner-channel layout onepipeline publishes for a host's onemessagebus to link.",
  "schemas": [
    {
      "id": "agent.planner-surface@1",
      "schema": {
        "type": "object",
        "properties": {
          "id": {
            "type": "integer",
            "format": "uint64",
            "minimum": 0,
            "description": "Monotonic within the run, so a consumer can report which one it read:\nallocated by the queue, one past the highest the log has queued."
          },
          "kind": {
            "type": "string",
            "description": "What the surface is asking about."
          },
          "message": {
            "type": "string",
            "description": "Its text."
          },
          "source": {
            "type": "string",
            "description": "What raised it — see [`source`]."
          },
          "blocking": {
            "type": "boolean",
            "description": "Whether the run is waiting on the answer. A **blocking** surface is a\ndecision point and holds the subtree that depends on\n[`workstream`](Self::workstream); a non-blocking one holds nothing."
          },
          "queued_at": {
            "type": "integer",
            "format": "uint64",
            "minimum": 0,
            "description": "When it was queued, in epoch milliseconds."
          },
          "workstream": {
            "type": [
              "string",
              "null"
            ],
            "description": "The node that provoked it, when one did."
          },
          "abandoned": {
            "type": "boolean",
            "description": "Whether anybody is listening for the answer.\n\nSet by the channel's `abandon` when the process serving this surface\nexited without an answer, and lifted by its `attend` when a later\nlistener of the same asker takes it back over — a listener ending is not\nthe asker going. While it stands, the surface keeps its text and its\nplace: what it gives up is its claim on the unread count, on the subtree\na blocking surface holds, and on being reported as a question the run\nawaits a verdict on. Omitted from the wire while it is false, so a queue\nnothing has abandoned serializes exactly as it always did."
          },
          "asker": {
            "anyOf": [
              {
                "$ref": "#/$defs/Asker"
              },
              {
                "type": "null"
              }
            ],
            "description": "Who raised it, when the session that did named an asker.\n\nThe key the channel's `attend` matches on: a later session of the same\nasker takes this surface back over, and a session of any other asker\nleaves it exactly where it is. `None` is a surface nobody named an asker\nfor — every one an older build wrote, and every one raised outside a\nserving session — and nothing ever adopts one of those. Omitted from the\nwire while it is absent, so a queue no asker was named on serializes\nexactly as it always did."
          },
          "correlation": {
            "anyOf": [
              {
                "$ref": "#/$defs/Correlation"
              },
              {
                "type": "null"
              }
            ],
            "description": "The correlation a reply echoes to answer it, when it was raised as a\nquestion through the bus's `ask`. Omitted while absent, so a surface\nraised any other way is written byte for byte as 0.28.2 wrote it."
          }
        },
        "required": [
          "id",
          "kind",
          "message",
          "source",
          "blocking",
          "queued_at"
        ],
        "description": "One surface, as it sits in the durable queue.\n\nThe record the `surfaces` queue holds, registered as `agent.planner-surface@1`\nand written in this field order — which is the order 0.28.2 wrote, and the\norder the bus reshapes every line it writes to.",
        "title": "Surface",
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "$defs": {
          "Asker": {
            "type": "string",
            "description": "One asker's name: the word by which two listeners are one side.\n\nA listener is something an asker rents, never the asker itself: an asker may\nraise a question through one listener and wait for its answer through a\nsuccession of them. Two listeners carrying the same asker are one asker, and\nthe later takes back over what the earlier left ([`RawQueue::attend`]).\n\nCompared for equality and never parsed. The two values that are not\nidentities are refused where a name enters: a **blank** one, which every\nlistener carrying it would match, and one that is **not Unicode**, which\ncollapses onto every other such value when read as text."
          },
          "Correlation": {
            "type": "string",
            "description": "Which question a reply answers: the token the bus minted when it was asked.",
            "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"
          }
        }
      }
    },
    {
      "id": "agent.queued-reply@1",
      "schema": {
        "type": "object",
        "properties": {
          "id": {
            "type": "integer",
            "format": "uint64",
            "minimum": 0,
            "description": "The number of replies before it."
          },
          "reply": {
            "$ref": "#/$defs/ReplyEnvelope",
            "description": "The envelope."
          },
          "at": {
            "type": "integer",
            "format": "uint64",
            "minimum": 0,
            "description": "When it was written, in epoch milliseconds."
          },
          "correlation": {
            "anyOf": [
              {
                "$ref": "#/$defs/Correlation"
              },
              {
                "type": "null"
              }
            ],
            "description": "The correlation of the question it answers, when it answers one asked\nthrough the bus's `ask`. Omitted while absent."
          }
        },
        "required": [
          "id",
          "reply",
          "at"
        ],
        "description": "One reply as `replies.jsonl` holds it: the record the layout registers as\n`agent.queued-reply@1`.",
        "title": "QueuedReply",
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "$defs": {
          "ReplyEnvelope": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "version": {
                "type": [
                  "integer",
                  "null"
                ],
                "format": "uint32",
                "minimum": 0,
                "description": "The version it was written against, read at [`REPLY_ENVELOPE_VERSION`]."
              },
              "author": {
                "$ref": "#/$defs/Author",
                "description": "Who wrote it. Omitted, the planner."
              },
              "completion": {
                "type": [
                  "boolean",
                  "null"
                ],
                "description": "The verdict: whether the author considers the run complete."
              },
              "message": {
                "type": [
                  "string",
                  "null"
                ],
                "description": "The verdict's message."
              },
              "reason": {
                "type": [
                  "string",
                  "null"
                ],
                "description": "Why the author reached that verdict."
              },
              "commands": {
                "type": "array",
                "items": {
                  "type": "object",
                  "additionalProperties": true
                },
                "description": "The graph edits, each an object naming its `op`."
              }
            },
            "description": "One reply envelope as the layout reads it: a verdict, a list of graph edits,\nor both.\n\nIts commands are carried as JSON, in the order and with the fields their\nauthor wrote: which ops exist and what each means is\n[`channel::Command`](crate::channel::Command)'s, and the layout reads only\neach command's `op`. The typed envelope the engine reconciles is\n[`channel::Reply`](crate::channel::Reply)."
          },
          "Author": {
            "type": "string",
            "description": "Who wrote a record: an open word."
          },
          "Correlation": {
            "type": "string",
            "description": "Which question a reply answers: the token the bus minted when it was asked.",
            "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$"
          }
        }
      }
    },
    {
      "id": "agent.queued-commands@1",
      "schema": {
        "type": "object",
        "properties": {
          "id": {
            "type": "integer",
            "format": "uint64",
            "minimum": 0,
            "description": "The number of envelopes before it."
          },
          "author": {
            "$ref": "#/$defs/Author",
            "description": "Who submitted it.",
            "default": "planner"
          },
          "commands": {
            "type": "array",
            "items": {
              "type": "object",
              "additionalProperties": true
            },
            "description": "The commands, each an object naming its `op`."
          }
        },
        "required": [
          "id",
          "commands"
        ],
        "description": "One command envelope as `commands.jsonl` holds it: the record the layout\nregisters as `agent.queued-commands@1`.",
        "title": "QueuedCommands",
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "$defs": {
          "Author": {
            "type": "string",
            "description": "Who wrote a record: an open word."
          }
        }
      }
    },
    {
      "id": "agent.command-outcome@1",
      "schema": {
        "type": "object",
        "properties": {
          "id": {
            "type": "integer",
            "format": "uint64",
            "minimum": 0,
            "description": "The envelope this answers."
          },
          "applied": {
            "type": "boolean",
            "description": "Whether every command in it was applied."
          },
          "reason": {
            "type": [
              "string",
              "null"
            ],
            "description": "Why not, when it was not."
          },
          "results": {
            "type": "array",
            "items": {
              "$ref": "#/$defs/CommandResult"
            },
            "description": "One entry per command the envelope carried, in the order it carried them.\n\nOmitted when empty, so a record this build writes for an envelope with no\ncommands is byte-for-byte the record an older build wrote."
          }
        },
        "required": [
          "id",
          "applied"
        ],
        "description": "The reconciler's answer to one submitted envelope, as\n`command-outcomes.jsonl` holds it: the record the layout registers as\n`agent.command-outcome@1`.\n\nAn envelope is all-or-nothing, so [`applied`](Self::applied) is still the\nwhole envelope's answer and every reader that predates\n[`results`](Self::results) keeps reading exactly what it read. What that\nboolean could never say is *which* command decided it, which is what left a\nmanager believing a node's bar had changed when the command that would have\nchanged it was never compiled.",
        "title": "CommandOutcome",
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "$defs": {
          "CommandResult": {
            "type": "object",
            "properties": {
              "index": {
                "type": "integer",
                "format": "uint",
                "minimum": 0,
                "description": "Where in the envelope's `commands` this one sat, from zero."
              },
              "op": {
                "type": "string",
                "description": "The command's op, as the envelope spelled it.\n\nCarried so an entry names the command it belongs to rather than leaving a\nreader to count positions in the envelope it sent."
              },
              "outcome": {
                "$ref": "#/$defs/CommandVerdict",
                "description": "What became of it."
              },
              "reason": {
                "type": [
                  "string",
                  "null"
                ],
                "description": "Why it refused, or what refused around it, when either happened."
              }
            },
            "required": [
              "index",
              "op",
              "outcome"
            ],
            "description": "What became of **one** command of an envelope.\n\nEvery command is evaluated, whatever the ones before it said, so each entry is\nthat command's **own** answer. The envelope is still atomic — a refusal\nanywhere in it applies none of it — and the four words of [`CommandVerdict`]\nare what tells apart the facts a single boolean could not: which commands\nwere wrong, which were fine and went down with them, and which of those had\nalready been read by a conversation that cannot unread it. A manager reading\nthem knows which to fix, which to resend unchanged, and which not to resend\nat all."
          },
          "CommandVerdict": {
            "oneOf": [
              {
                "type": "string",
                "const": "applied",
                "description": "It was validated and committed."
              },
              {
                "type": "string",
                "const": "validated",
                "description": "It was validated and nothing was wrong with it, and **nothing of it\nhappened**: no conversation was offered anything on its behalf, no graph\nmoved, and no record was written for it. Something else in the envelope\nrefused, and an envelope applies all of its commands or none. Resending it\non its own is what gets it in, and resending it costs nothing, because it\nhad no effect to repeat."
              },
              {
                "type": "string",
                "const": "delivered",
                "description": "A `note` whose conversation **took it**, in an envelope refused after that.\n\nNothing of this command was committed — the same nothing\n[`Validated`](Self::Validated) reports — but a conversation has no undo, so\nresending the envelope hands that party the note a second time.\n`engine::deliver_envelope` states the one window this is reachable through."
              },
              {
                "type": "string",
                "const": "refused",
                "description": "It refused, and [`reason`](CommandResult::reason) is what it said."
              }
            ],
            "description": "The four things that can become of one command of an envelope.\n\nOne field rather than a boolean and a sentence, because \"not applied\" was two\nfacts wearing one word: a command that was wrong and a command that was fine.\nA reader that cannot tell them apart resends the wrong one and fixes the right\none."
          }
        }
      }
    },
    {
      "id": "agent.planner-reply-envelope@1",
      "schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "version": {
            "type": [
              "integer",
              "null"
            ],
            "format": "uint32",
            "minimum": 0,
            "description": "The version it was written against, read at [`REPLY_ENVELOPE_VERSION`]."
          },
          "author": {
            "$ref": "#/$defs/Author",
            "description": "Who wrote it. Omitted, the planner."
          },
          "completion": {
            "type": [
              "boolean",
              "null"
            ],
            "description": "The verdict: whether the author considers the run complete."
          },
          "message": {
            "type": [
              "string",
              "null"
            ],
            "description": "The verdict's message."
          },
          "reason": {
            "type": [
              "string",
              "null"
            ],
            "description": "Why the author reached that verdict."
          },
          "commands": {
            "type": "array",
            "items": {
              "type": "object",
              "additionalProperties": true
            },
            "description": "The graph edits, each an object naming its `op`."
          }
        },
        "description": "One reply envelope as the layout reads it: a verdict, a list of graph edits,\nor both.\n\nIts commands are carried as JSON, in the order and with the fields their\nauthor wrote: which ops exist and what each means is\n[`channel::Command`](crate::channel::Command)'s, and the layout reads only\neach command's `op`. The typed envelope the engine reconciles is\n[`channel::Reply`](crate::channel::Reply).",
        "title": "ReplyEnvelope",
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "$defs": {
          "Author": {
            "type": "string",
            "description": "Who wrote a record: an open word."
          }
        }
      }
    },
    {
      "id": "agent.reply-envelope@2",
      "schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "title": "ReplyEnvelope",
        "description": "A reply on the planner channel at version 2: the verdict half (completion, message, reason) and the command half (commands), either or both. The shape is onepipeline's; the meaning of each command's op is onepipeline's to define.",
        "type": "object",
        "properties": {
          "version": {
            "description": "The reply envelope version this document was written against.",
            "type": "integer",
            "const": 2
          },
          "author": {
            "description": "Who wrote the reply. Omitted, the planner.",
            "type": "string"
          },
          "completion": {
            "description": "The verdict: whether the author considers the run complete.",
            "type": "boolean"
          },
          "message": {
            "description": "The verdict's message to the orchestrator.",
            "type": "string"
          },
          "reason": {
            "description": "Why the author reached that verdict.",
            "type": "string"
          },
          "commands": {
            "description": "The graph edits, reconciled in order.",
            "type": "array",
            "items": {
              "$ref": "#/$defs/Command"
            }
          }
        },
        "additionalProperties": false,
        "$defs": {
          "Command": {
            "description": "One graph edit, discriminated by op. Its other fields are the op's own.",
            "type": "object",
            "properties": {
              "op": {
                "description": "Which edit this is.",
                "type": "string"
              }
            },
            "required": [
              "op"
            ]
          }
        }
      }
    },
    {
      "id": "agent.reply-envelope@3",
      "schema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "title": "ReplyEnvelope",
        "description": "A reply on the planner channel at version 3: version 2 plus one optional field, a settle command's landing. Additive, so an envelope written against 2 is a complete envelope at 3.",
        "type": "object",
        "properties": {
          "version": {
            "description": "The reply envelope version this document was written against.",
            "type": "integer",
            "const": 3
          },
          "author": {
            "description": "Who wrote the reply. Omitted, the planner.",
            "type": "string"
          },
          "completion": {
            "description": "The verdict: whether the author considers the run complete.",
            "type": "boolean"
          },
          "message": {
            "description": "The verdict's message to the orchestrator.",
            "type": "string"
          },
          "reason": {
            "description": "Why the author reached that verdict.",
            "type": "string"
          },
          "commands": {
            "description": "The graph edits, reconciled in order.",
            "type": "array",
            "items": {
              "$ref": "#/$defs/Command"
            }
          }
        },
        "additionalProperties": false,
        "$defs": {
          "Command": {
            "description": "One graph edit, discriminated by op. Its other fields are the op's own; a settle may name the landing it was decided from.",
            "type": "object",
            "properties": {
              "op": {
                "description": "Which edit this is.",
                "type": "string"
              },
              "landing": {
                "description": "Where a settled node's work landed: a change request URL or a commit id.",
                "type": "string"
              }
            },
            "required": [
              "op"
            ]
          }
        }
      }
    }
  ],
  "layouts": [
    {
      "name": "planner-channel",
      "description": "onepipeline's planner channel: the surfaces a planner is asked about (`surfaces`, answered on `replies`), the replies and command envelopes it writes, and what each envelope was answered with. Generated from the layout this engine compiles in; a surface of source `check-in` supersedes a waiting one.",
      "queues": [
        {
          "name": "surfaces",
          "policy": {
            "delivery": "at-least-once",
            "ordering": "per-queue",
            "supersede_on": {
              "key": "source",
              "when": {
                "field": "source",
                "equals": "check-in"
              }
            },
            "hold_pending": true,
            "blocking_first": true,
            "retention": "keep",
            "projection": "queue.json"
          },
          "schema": "agent.planner-surface@1",
          "answers": "replies",
          "consumers": [
            "default"
          ],
          "numbered": false
        },
        {
          "name": "replies",
          "policy": {
            "delivery": "at-least-once",
            "ordering": "per-queue",
            "hold_pending": false,
            "blocking_first": false,
            "retention": "keep"
          },
          "schema": "agent.queued-reply@1",
          "claims": {
            "not": {
              "all": [
                {
                  "field": "reply.commands",
                  "non_empty": true
                },
                {
                  "not": {
                    "any": [
                      {
                        "field": "reply.completion",
                        "present": true
                      },
                      {
                        "field": "reply.message",
                        "present": true
                      },
                      {
                        "field": "reply.reason",
                        "present": true
                      }
                    ]
                  }
                }
              ]
            }
          },
          "consumers": [
            "default"
          ],
          "numbered": true
        },
        {
          "name": "commands",
          "policy": {
            "delivery": "at-least-once",
            "ordering": "per-queue",
            "hold_pending": false,
            "blocking_first": false,
            "retention": "keep"
          },
          "schema": "agent.queued-commands@1",
          "consumers": [
            "default"
          ],
          "numbered": true
        },
        {
          "name": "command-outcomes",
          "policy": {
            "delivery": "at-least-once",
            "ordering": "per-queue",
            "hold_pending": false,
            "blocking_first": false,
            "retention": "keep"
          },
          "schema": "agent.command-outcome@1",
          "consumers": [
            "default"
          ],
          "numbered": false
        }
      ],
      "operations": [
        "set-node-sets",
        "set-run-node-sets",
        "add",
        "drop",
        "reparent",
        "retry",
        "cancel",
        "requeue",
        "complete",
        "attest",
        "finding",
        "amend",
        "note",
        "settle"
      ],
      "authors": {
        "planner": {
          "every_op": true
        }
      },
      "prepare": {
        "commands": [
          {
            "grant": {
              "author": "author",
              "default_author": "planner",
              "each": "commands",
              "op": "op",
              "refusal": "'{op}' is not an op the {author} may issue: {reason}. Surface it to the planner instead",
              "unknown": "'{op}' is not an op of the planner channel; the ops are: {ops}",
              "undeclared": "the envelope's author `{author}` is not declared; the declared authors are: {authors}",
              "malformed": "the envelope's author: {why}"
            }
          }
        ],
        "replies": [
          {
            "check": {
              "schema": "agent.planner-reply-envelope@1",
              "at": "reply",
              "refusal": "the reply is malformed: {why}",
              "when": {
                "field": "reply",
                "present": true
              }
            }
          },
          {
            "grant": {
              "author": "reply.author",
              "default_author": "planner",
              "refusal": "'{op}' is not an op the {author} may issue: {reason}. Surface it to the planner instead",
              "unknown": "'{op}' is not an op of the planner channel; the ops are: {ops}",
              "undeclared": "the envelope's author `{author}` is not declared; the declared authors are: {authors}",
              "malformed": "the envelope's author: {why}",
              "when": {
                "field": "reply",
                "present": true
              }
            }
          },
          {
            "grant": {
              "author": "reply.author",
              "default_author": "planner",
              "word": "complete",
              "refusal": "declaring the run complete is not something the {author} may do: {reason}. Surface it to the planner instead",
              "unknown": "'{op}' is not an op of the planner channel; the ops are: {ops}",
              "undeclared": "the envelope's author `{author}` is not declared; the declared authors are: {authors}",
              "malformed": "the envelope's author: {why}",
              "when": {
                "all": [
                  {
                    "field": "reply",
                    "present": true
                  },
                  {
                    "field": "reply.completion",
                    "equals": true
                  }
                ]
              }
            }
          },
          {
            "version": {
              "at": "reply.version",
              "value": 3,
              "required_when": {
                "all": [
                  {
                    "field": "reply.commands",
                    "non_empty": true
                  },
                  {
                    "not": {
                      "any": [
                        {
                          "field": "reply.version",
                          "equals": 2
                        }
                      ]
                    }
                  }
                ]
              },
              "refusal": "an edit envelope requires version {value}",
              "when": {
                "field": "reply",
                "present": true
              }
            }
          },
          {
            "grant": {
              "author": "reply.author",
              "default_author": "planner",
              "each": "reply.commands",
              "op": "op",
              "refusal": "'{op}' is not an op the {author} may issue: {reason}. Surface it to the planner instead",
              "unknown": "'{op}' is not an op of the planner channel; the ops are: {ops}",
              "undeclared": "the envelope's author `{author}` is not declared; the declared authors are: {authors}",
              "malformed": "the envelope's author: {why}",
              "when": {
                "field": "reply",
                "present": true
              }
            }
          },
          {
            "check": {
              "schema": "agent.planner-reply-envelope@1",
              "refusal": "the reply is malformed: {why}",
              "when": {
                "not": {
                  "field": "reply",
                  "present": true
                }
              }
            }
          },
          {
            "grant": {
              "author": "author",
              "default_author": "planner",
              "refusal": "'{op}' is not an op the {author} may issue: {reason}. Surface it to the planner instead",
              "unknown": "'{op}' is not an op of the planner channel; the ops are: {ops}",
              "undeclared": "the envelope's author `{author}` is not declared; the declared authors are: {authors}",
              "malformed": "the envelope's author: {why}",
              "when": {
                "not": {
                  "field": "reply",
                  "present": true
                }
              }
            }
          },
          {
            "grant": {
              "author": "author",
              "default_author": "planner",
              "word": "complete",
              "refusal": "declaring the run complete is not something the {author} may do: {reason}. Surface it to the planner instead",
              "unknown": "'{op}' is not an op of the planner channel; the ops are: {ops}",
              "undeclared": "the envelope's author `{author}` is not declared; the declared authors are: {authors}",
              "malformed": "the envelope's author: {why}",
              "when": {
                "all": [
                  {
                    "not": {
                      "field": "reply",
                      "present": true
                    }
                  },
                  {
                    "field": "completion",
                    "equals": true
                  }
                ]
              }
            }
          },
          {
            "version": {
              "at": "version",
              "value": 3,
              "reads": [
                2
              ],
              "required_when": {
                "field": "commands",
                "non_empty": true
              },
              "refusal": "an edit envelope requires version {value}",
              "when": {
                "not": {
                  "field": "reply",
                  "present": true
                }
              }
            }
          },
          {
            "grant": {
              "author": "author",
              "default_author": "planner",
              "each": "commands",
              "op": "op",
              "refusal": "'{op}' is not an op the {author} may issue: {reason}. Surface it to the planner instead",
              "unknown": "'{op}' is not an op of the planner channel; the ops are: {ops}",
              "undeclared": "the envelope's author `{author}` is not declared; the declared authors are: {authors}",
              "malformed": "the envelope's author: {why}",
              "when": {
                "not": {
                  "field": "reply",
                  "present": true
                }
              }
            }
          },
          {
            "route": {
              "routes": [
                {
                  "queue": "commands",
                  "on": [
                    "commands"
                  ],
                  "take": [
                    "author",
                    "commands"
                  ]
                },
                {
                  "queue": "replies",
                  "on": [
                    "completion",
                    "message",
                    "reason"
                  ],
                  "take": [
                    "version",
                    "author",
                    "completion",
                    "message",
                    "reason",
                    "commands"
                  ],
                  "under": "reply",
                  "stamp": [
                    "at"
                  ]
                }
              ],
              "fallback": "replies",
              "when": {
                "not": {
                  "field": "reply",
                  "present": true
                }
              }
            }
          }
        ],
        "surfaces": [
          {
            "rename": {
              "from": "about",
              "to": "workstream"
            }
          },
          {
            "stamp": {
              "member": "queued_at"
            }
          }
        ]
      }
    }
  ]
}
