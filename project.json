{
  "name": "onepipeline",
  "$schema": "node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "src",
  "tags": [
    "scope:cli",
    "lang:rust"
  ],
  "targets": {
    "bootstrap": {
      "command": "just _crate-bootstrap"
    },
    "build": {
      "command": "just _crate-build",
      "inputs": [
        "crateSource"
      ]
    },
    "format": {
      "command": "just _crate-format"
    },
    "format-check": {
      "command": "just _crate-fmt-check",
      "inputs": [
        "crateSource"
      ]
    },
    "lint": {
      "command": "just _crate-lint",
      "inputs": [
        "crateSource"
      ]
    },
    "coverage-clean": {
      "command": "just _crate-coverage-clean"
    },
    "test-rest": {
      "command": "just _crate-test-rest",
      "inputs": [
        "crateSource"
      ],
      "dependsOn": [
        "coverage-clean"
      ]
    },
    "test": {
      "command": "just _crate-coverage",
      "inputs": [
        "crateSource"
      ],
      "dependsOn": [
        "test-rest",
        {
          "projects": [
            "onepipeline-note-journeys"
          ],
          "target": "test"
        }
      ]
    },
    "doc": {
      "command": "just _crate-doc",
      "inputs": [
        "crateSource"
      ]
    },
    "lint-llm-diff": {
      "command": "bash scripts/llmlint-judge.sh",
      "options": {
        "cwd": "."
      }
    },
    "check": {
      "executor": "nx:noop",
      "dependsOn": [
        "format-check",
        "lint",
        "test",
        "doc"
      ]
    }
  },
  "implicitDependencies": [
    "onepipeline-note-journeys"
  ]
}
